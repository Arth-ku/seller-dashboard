SELLER_DASHBOARD_HOST=0.0.0.0
SELLER_DASHBOARD_PORT=8000
SELLER_DASHBOARD_BASE_PATH=/sell
SELLER_PUBLIC_ALLOWED_ORIGIN=https://auth.example.com
# Admin password for the dashboard. If unset, the dashboard is open to anyone who can reach it.
# Keep the real value in /etc/seller-dashboard/app.env on the Pi, never commit it.
SELLER_ADMIN_PASSWORD=change-me

# Optional token for a separate Lucy bot/worker to publish to /api/lucy/insights
# without a browser admin cookie. Keep the real value in /etc/seller-dashboard/app.env.
LUCY_WRITE_TOKEN=

# Optional daily automated CSV import.
# Use a Google Sheets CSV export URL or a published-to-web CSV URL.
# Example:
# SELLER_GOOGLE_SHEET_CSV_URL=https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/export?format=csv&gid=SHEET_GID
SELLER_GOOGLE_SHEET_CSV_URL=
SELLER_GOOGLE_SHEET_NAME=Sale dashboard Google Sheet

# Optional order review/refund process import overrides. The app defaults to the documented
# shared order-process sheet when these values are empty or unset.
# SELLER_ORDER_PROCESS_SHEET_CSV_URL=https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/export?format=csv
SELLER_ORDER_PROCESS_SHEET_NAME=Order review process
